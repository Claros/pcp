{% extends '::layout.html.twig' %}

{% block content %}

    <h1 class="ui header">
        Nouveau BV
    </h1>
    <form class="ui green form segment" method="post" {{ form_enctype(form) }} novalidate>
        {{ form_widget(form) }}
        <button type="submit" class="ui green labeled icon submit button">
            Créer <i class="plus icon"></i>
        </button>
        <a href="{{ path('je_finances_bv') }}" class="ui labeled icon submit button">
            Annuler <i class="reply mail icon"></i>
        </a>
    </form>
{% endblock %}